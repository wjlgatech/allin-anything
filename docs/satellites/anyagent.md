# Satellite digest — anyagent

- **Upstream:** https://github.com/wjlgatech/anyagent (PRIVATE — pointer only, source never exposed here)
- **Role here:** ⚙️ engine, 🟢 integrated — the goal-10x front door (`anyagent goal`) drove this repo's
  own build; `anyagent analyze` scores it at each milestone.
- **Verified locally (2026-07-25):** `anyagent 0.1.0` installed; `goal` routing + reflect loop live.

## Integration evidence

- Routed in `skills/allin-anything/SKILL.md` (routing table).
- This repo's ROADMAP milestones are scored with `anyagent analyze` (see docs/ROADMAP.md DoD column).
