# Satellite digest — animate-anything

- **Pinned (local HEAD, 2026-08-01):** `946bef8529f1`
- **Upstream:** https://github.com/wjlgatech/animate-anything (PUBLIC)
- **Role here:** 💻 digital — the family's motion-craft organ; joined the registry 2026-07-27 as a
  ⚪ candidate when the router found the gap (a learn-with-animations intent had no animation
  satellite to route to).

## Confirmed facts (repo @ pin)

- The ranked, living map of animation — CSS keyframes to AI-authored video; machine-readable
  registry for agents; interactive map on GitHub Pages.
- Craft rules are executable, not vibes: `scripts/animate.py lint` scores a scene against ten
  3b1b-style checks (warm-grey canvas, palette-by-role, morph-to-equivalence, pacing beats,
  one-idea-per-play, narrative arc) with a CI gate at 80.

## Live gate evidence (2026-08-01)

- `make check` run here on the pinned checkout: **exit 0** — style-linter gate scored the example
  scene **100/100 (10/10 checks)**. That run is why this satellite is 🟢, not the description above.
- Its craft already shipped through this repo once before the promotion: the pure-CSS session
  animations in `examples/fields-2026/` follow its rules (see Chain 06 walkthrough).

## Honest gaps

- The gate lints animation *source* for craft conformance; it does not render video (manim not
  required for the gate). Rendered-output quality remains human-judged.
