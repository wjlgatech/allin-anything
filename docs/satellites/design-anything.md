# Satellite digest — design-anything

- **Pinned (local HEAD, 2026-07-25):** `eb48a6f78f0c` (v0.13.0)
- **Upstream:** https://github.com/wjlgatech/design-anything (PRIVATE on GitHub — pointer)
- **Role here:** 🦾 physical — the family's execution-ready physical-blueprint organ, and the
  physical end of the first verified digital↔physical chain (see docs/walkthroughs/).

## Confirmed facts (verified by RUNNING it, 2026-07-25)

- `make check` exit 0 in 7.7s: validate + tests + drift gates + satellite freshness + vertical-
  slice gates + ainative self-audit 100/100 (12/12 principles).
- Three domain gates run real geometry: `ready_gate` (STL, min-feature 3.0mm), `construction_gate`
  (floor-plan JSON: topology, clearances, habitability, egress, 100mm module grid),
  `pattern_gate` (garment JSON, graded sizes, seam pairs).
- Studio-flat example: 4 rooms, 28.5 m², 5000×5700mm — passes C1–C5, "READY" with the honest
  banner "design-sanity gate, not a permit or PE stamp".
- Same playbook lineage: SHA-pinned satellites (TRELLIS.2, blender-mcp, SpatialLM, Seamly2D,
  GarmentCode), drift-gated README/news, golden examples banked from live runs.

## Unconfirmed

- GitHub Actions state upstream (checked only locally at this pin).
